<h3 class="">{{"title.profile.password.edit"|trans}}</h3>
{{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
    {{ form_widget(form) }}
    <div>
        <button type="submit" class="btn btn-primary btn-lg btn-block">{{ 'button.update'|trans }}</button>
    </div>
{{ form_end(form) }}
